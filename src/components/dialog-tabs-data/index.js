import DialogTabsData from './dialog-tabs-data.vue';

/* istanbul ignore next */
DialogTabsData.install = function (Vue) {
    Vue.component(DialogTabsData.name, DialogTabsData);
};

export default DialogTabsData;